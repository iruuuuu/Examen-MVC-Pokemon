<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle de Pokémon: <?= $pokemon->getNombre() ?></title>
    <link rel="stylesheet" href="public/css/style.css">
    <link rel="stylesheet" href="public/css/pokemon-grid.css">
</head>
<body>
    <div class="container">
        <div class="user-bar">
            <?php if (isset($_SESSION['user'])): ?>
                <div class="user-info">
                    <img src="public/img/avatars/<?= $_SESSION['user']->getAvatar() ?: 'avatar.jpg' ?>" alt="Avatar">
                    <span>Hola, <strong><?= $_SESSION['user']->getNombre() ?></strong></span>
                </div>
                <div class="user-actions">
                    <a href="index.php">Volver al inicio</a>
                    <a href="index.php?c=usuarios&action=logout">Logout</a>
                </div>
            <?php else: ?>
                <a href="index.php">Volver al inicio</a>
            <?php endif; ?>
        </div>

        <div class="pokemon-detail-card">
            <img src="public/img/pokemon/<?= $pokemon->getFoto() ?>" alt="<?= $pokemon->getNombre() ?>">
            <h2><?= $pokemon->getNombre() ?></h2>
            <p><strong>Tipo:</strong> <?= ucfirst($pokemon->getTipo()) ?></p>

            <div class="pokemon-actions">
                <?php if (isset($_SESSION['user']) && $capturado): ?>
                    <a href="index.php?c=pokedex&action=quitar&id=<?= $pokemon->getId() ?>" class="btn-delete">Quitar de mi Pokédex</a>
                <?php elseif (isset($_SESSION['user'])): ?>
                    <a href="index.php?c=pokedex&action=capturar&id=<?= $pokemon->getId() ?>" class="btn-create">Capturar Pokémon</a>
                <?php endif; ?>

                <?php if (isset($_SESSION['user']) && $_SESSION['user']->getRol() == 'admin'): ?>
                    <div class="admin-actions" style="margin-top: 10px;">
                        <a href="index.php?c=pokemon&action=editar&id=<?= $pokemon->getId() ?>" class="btn-edit">Editar</a>
                        <a href="index.php?c=pokemon&action=eliminar&id=<?= $pokemon->getId() ?>" class="btn-delete" onclick="return confirm('¿Estás seguro de que quieres eliminar a <?= $pokemon->getNombre() ?>?');">Eliminar</a>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</body>
</html>